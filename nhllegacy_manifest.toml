# Generated by ReXGlue v0.8.0 on 2026-06-04 02:59:22 UTC.

[project]
name = "nhllegacy"
sdk_version = "0.8.0"
game_root = "h:/emulators/games/xbox/nhl legacy - vanilla"

[entrypoint]
file_path = "h:/emulators/games/xbox/nhl legacy - vanilla/default.xex"
out_directory_path = "generated/default"
includes = ["nhllegacy_functions.toml"]
# EA CRT setjmp/longjmp pair (exception handling with C++ EH disabled).
# setjmp 0x830801C0: optional-hook slot check (0x83E022E0, statically zero,
# no installer anywhere in the image) then full f14-f31/r13-r31/VMX128 save;
# the recompiler treated the hook's bnectr as a terminator and dropped the
# save body, so setjmp returned its r3 (jmp_buf ptr, nonzero) and every
# caller took the "longjmp returned an error" path -> boot self-destruct.
# longjmp 0x8307E1B0: matching restore (lfd f14,0(r7)..., ld r13,0x98...).
setjmp_address = 0x830801C0
longjmp_address = 0x8307E1B0
